========================================
  完成迁移的命令
========================================

已完成:
✓ 文件已复制到 data/csrankings/
✓ 代码已更新为使用新路径

请在命令行执行以下命令:

1. 删除CSRankings目录
   rmdir /s /q CSRankings

2. 添加文件到git
   git add data/csrankings/
   git add scripts/
   git add .gitignore
   git add GITHUB_PUSH.md
   git add README.md
   git add start.bat
   git add start.sh

3. 提交
   git commit -m "Fix: Move CSRankings files to data/csrankings/ to avoid submodule issue"

4. 推送
   git push

5. 删除此文件
   del COMMIT_CHANGES.txt
   del finalize-migration.bat

完成后CSRankings目录在GitHub上就可以正常浏览了！

